import React from 'react';

const Hero = () => (
  <section className="hero">
    <h1>Community Disaster Management System</h1>
    <p>Providing help and support during times of need</p>
    {/* Button that scrolls to the Volunteer Registration section */}
    <a href="#volunteer" className="cta-button">Get Involved</a>
  </section>
);

export default Hero;
